\chapter{HOPF-01: Chiral Antenna Verification}
\label{ch:hopf_01}

Before constructing a mechanical thrust measurement, the AVE framework offers a purely electromagnetic falsification test that requires nothing more than a printed circuit board, enameled magnet wire, and a vector network analyzer. If the vacuum possesses intrinsic chirality (Axiom~1), then the resonant frequency of a torus knot antenna must deviate from the standard Maxwell prediction by an amount that scales exactly with the knot's topological winding number.

\section{The Chiral Coupling Prediction}

An open-ended wire resonator of length $L_{trace}$ in a medium with effective permittivity $\varepsilon_{eff}$ resonates at:
\begin{equation}
    f_{std} = \frac{c}{2 L_{trace} \sqrt{\varepsilon_{eff}}}
\end{equation}
This is the prediction for a \textit{straight, open-ended} wire resonator. For the actual 3D torus knot geometry, curvature and inter-segment coupling will cause deviations from this formula even in standard Maxwell theory. However, the AVE prediction is not about the absolute frequency, but the \textit{scaling law} across multiple knot topologies on the same board.

The AVE framework predicts an additional correction. Because the $(p,q)$ torus knot topology couples to the intrinsic chirality of the $\mathcal{M}_A$ lattice, the effective refractive index acquires a topological term:
\begin{equation}
    n_{AVE} = \sqrt{\varepsilon_{eff}}\left(1 + \alpha \frac{pq}{p+q}\right)
    \label{eq:n_ave}
\end{equation}
where $\alpha \approx 1/137$ is the fine-structure constant and $pq/(p+q)$ is the harmonic mean of the torus knot winding numbers. This is \textbf{not a free parameter}: $\alpha$ is fixed by Axiom~2, and $p,q$ are fixed by the physical wire geometry.

The resulting frequency shift is:
\begin{equation}
    \frac{\Delta f}{f_{std}} = \alpha \frac{pq}{p+q}
\end{equation}

\section{Wire-Stitched Torus Knot Fixture}

The PCB serves as a mechanical fixture, not the antenna itself. Enameled magnet wire (24~AWG, 0.51~mm diameter) is threaded through unplated drill holes spaced 3~mm apart along each knot path, creating true 3D torus knots with real over/under crossings.

The standard 2-layer FR-4 board uses a ground-patch architecture: F.Cu copper ground patches (12$\times$12~mm) sit under each SMA connector and connect to a continuous F.Cu perimeter ground trace (0.5~mm wide, at 5~mm inset from the board edge). No ground vias are required---the SMA thru-hole pads inherently bridge both copper layers. B.Cu is bare, allowing the wire to route freely on both sides of the board. The board is elevated on 10~mm nylon standoffs, providing clearance for under-crossings.

Because the wire has no continuous ground plane underneath, it resonates as a \textbf{free-space wire resonator} rather than a microstrip line. The effective permittivity is dominated by air with a small correction from the polyurethane enamel coating ($\varepsilon_{enamel} \approx 3.5$, 30~$\mu$m thick), yielding $\varepsilon_{eff} \approx 1.295$.

All five knot topologies plus one zero-topology control antenna share a \textbf{single 160$\times$120~mm FR-4 panel} in a 3$\times$2 grid layout. Silkscreen markings provide a winding guide with crossing markers (OVER/UNDER) at each self-intersection point.

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{hopf_01_impedance_model.png}
    \caption{\textbf{HOPF-01 Impedance and Frequency Model.} Top row: predicted S$_{11}$ response in air (left) and mineral oil (right), showing resonant dips for all five knots with SMA mismatch. Middle left: characteristic impedance $Z_0$ versus wire height for both media. Middle right: skin depth in 24~AWG copper wire. Bottom left: the chiral scaling law $\Delta f/f = \alpha \cdot pq/(p+q)$ verified as substrate-independent. Bottom right: model summary.}
    \label{fig:hopf_01_impedance}
\end{figure}

The predicted shifts for the enamel-corrected wire-in-air model ($\varepsilon_{eff} = 1.295$) are:

\begin{center}
\begin{tabular}{|c|c|c|c|c|c|c|}
\hline
Torus Knot & $pq/(p{+}q)$ & $L_{wire}$ & $f_{std}$ (GHz) & $\Delta f$ (MHz) & $Q$ & Shift (ppm) \\ \hline
$(2,3)$ Trefoil    & 1.200 & 120 mm & 1.098 &  9.5 & 681 & 8{,}681  \\
$(2,5)$ Cinquefoil & 1.429 & 160 mm & 0.823 &  8.5 & 590 & 10{,}317 \\
$(3,5)$            & 1.875 & 170 mm & 0.776 &  10.6 & 566 & 13{,}505 \\
$(3,7)$            & 2.100 & 200 mm & 0.659 & 10.0 & 527 & 15{,}093 \\
$(3,11)$           & 2.357 & 250 mm & 0.527 &  8.9 & 471 & 16{,}910 \\ \hline
\end{tabular}
\end{center}

All shifts exceed 8.5~MHz---easily resolvable with a VNA. All resonant frequencies fall below 1.1~GHz (well within NanoVNA-H4 range). The $(3,5)$ knot provides an intermediate data point (five knots plus the zero-topology control give six independent measurements). When submerged in mineral oil ($\varepsilon_{eff} \approx 2.265$), frequencies drop to 0.40--0.83~GHz.

\section{The Falsification Protocol}

\begin{enumerate}
    \item \textbf{Fabrication:} Order a single 160$\times$120~mm, 2-layer FR-4 panel from JLCPCB ($\varepsilon_r = 4.3 \pm 0.05$, 1.6~mm thickness, 1~oz Cu, ENIG finish) with unplated stitching holes. Thread 24~AWG enameled magnet wire through the holes following the silkscreen guide, creating five 3D torus knots plus the meander control. Solder wire starts to the SMA feed pads.

    \item \textbf{Calibration:} Perform SOL (Short-Open-Load) calibration of the VNA at the SMA reference plane.

    \item \textbf{Measurement~(Air):} Sweep each of the six antennas individually in air. Record $f_{res}$ (the deepest S$_{11}$ dip below $-10$~dB). Repeat 10 times per antenna, rotating the cable to average connector noise.

    \item \textbf{Measurement~(Oil):} Submerge the entire board in a glass dish of mineral oil ($\varepsilon_r \approx 2.1$, transformer grade). Re-measure all six antennas. The oil changes the wave speed but \textbf{not} the topology.

    \item \textbf{Extract the Anomaly:} For each knot and each medium, compute:
    \begin{equation}
        \Delta f_i = f_{measured,i} - f_{Maxwell,i}
    \end{equation}
    where $f_{Maxwell}$ is the standard prediction using the measured $\varepsilon_{eff}$.

    \item \textbf{Test the Scaling Law:} Plot $\Delta f / f$ vs.\ $pq/(p+q)$ for all five knots (plus the $pq/(p+q)=0$ control point).
    \begin{itemize}
        \item \textbf{AVE confirmed:} $\Delta f/f$ is linear through the origin with slope $\alpha$, and \textit{identical} in air and oil.
        \item \textbf{AVE falsified:} $\Delta f/f$ is zero, random, does not scale as $pq/(p+q)$, or differs between media.
    \end{itemize}

    \item \textbf{Substrate Independence:} The air vs.\ mineral oil comparison replaces the need for multiple PCB substrates (Rogers, duroid). The fractional shift $\Delta f / f$ must be identical in both media---it depends only on $\alpha$ and $pq/(p+q)$, not on $\varepsilon_{eff}$.
\end{enumerate}

\section{Impedance Characterization}

The characteristic impedance of a round wire at height $h$ above a ground plane is given by the image-charge model:
\begin{equation}
    Z_0 = \frac{60}{\sqrt{\varepsilon_{eff}}} \operatorname{acosh}\!\left(\frac{2h}{d}\right)
    \label{eq:z0_wire}
\end{equation}
where $d = 0.51$~mm is the wire diameter. Near the SMA feed point ($h \approx 1.86$~mm above the B.Cu ground patch), $Z_0 \approx 141\;\Omega$ in air and $107\;\Omega$ in mineral oil. The SMA-to-wire impedance mismatch produces a reflection coefficient $\Gamma = (Z_0 - 50)/(Z_0 + 50) \approx 0.48$ (return loss $\approx 6.4$~dB). This raises the S$_{11}$ baseline but does \textbf{not} shift the resonant frequency.

Away from the SMA patches, the wire has no continuous ground plane and behaves as a free-space resonator. The quality factor is limited by AC resistance at the operating frequency:
\begin{equation}
    Q = \frac{\pi Z_0}{R_{ac} \cdot L_{wire}}, \quad R_{ac} = \frac{1}{\sigma_{Cu} \cdot A_{skin}}
\end{equation}
where $A_{skin}$ is the skin-depth--limited cross-sectional area. At $\sim$1~GHz, the skin depth in copper is $\sim$2~$\mu$m, yielding $Q \approx 470$--$680$ depending on wire length.

\section{Wire-Stitched Knot Geometry}

Each torus knot is generated from the 3D parametric equations $x(t) = (R + r\cos qt)\cos pt$, $y(t) = (R + r\cos qt)\sin pt$, $z(t) = r\sin qt$, then projected onto the PCB plane. Unplated drill holes (1.0~mm) are placed every 3~mm along the projected curve, and crossing points are identified from the $z$-coordinates: the strand with higher $z$ at each crossing passes \textbf{over} the board (in air), while the other passes \textbf{under} (through a hole). Silkscreen labels mark each crossing as OVER or UNDER.

\subsection{Wire Length Derivation from Knot Topology}

The wire length for each antenna is \textbf{not a free parameter}---it is a deterministic consequence of the knot topology $(p,q)$ at a fixed torus aspect ratio. The arc length of a $(p,q)$ torus knot with major radius $R$ and minor radius $r$ is:
\begin{equation}
    L_{wire} = \int_0^{2\pi} \sqrt{\left(\frac{dx}{dt}\right)^2 + \left(\frac{dy}{dt}\right)^2 + \left(\frac{dz}{dt}\right)^2}\; dt
    \label{eq:arc_length}
\end{equation}
This integral depends only on $p$, $q$, and the aspect ratio $R/r$. The PCB design fixes $R/r = 2.5$ (values $R = 1.0$, $r = 0.4$ in normalized units), chosen as the minimum ratio that keeps adjacent windings separated enough for the 1.0~mm stitching holes to be resolvable by hand. The physical scale is then set by requiring the resulting torus to fit within a PCB grid cell ($\sim$40~mm column spacing), yielding footprints of 21--25~mm.

\begin{center}
\begin{tabular}{|c|c|c|c|c|}
\hline
Knot & $L_{arc}$ (norm) & Scale (mm) & $L_{wire}$ (mm) & Footprint (mm) \\ \hline
$(2,3)$  & 14.78 & 8.12 & 120 & 23 \\
$(2,5)$  & 17.95 & 8.91 & 160 & 25 \\
$(3,5)$  & 22.86 & 7.44 & 170 & 21 \\
$(3,7)$  & 26.05 & 7.68 & 200 & 21 \\
$(3,11)$ & 33.75 & 7.41 & 250 & 21 \\ \hline
\end{tabular}
\end{center}

\noindent The second column is the dimensionless arc length at unit scale ($R=1$, $r=0.4$), computed numerically by the PCB generator (\texttt{scale\_knot\_to\_length}). The third column is the physical scaling factor (mm per normalized unit). The wire lengths monotonically increase with topological complexity because more windings produce longer curves, but the physical torus stays at approximately the same size ($\sim$22~mm diameter). This is verified by running:\\[4pt]
\texttt{PYTHONPATH=src python hardware/hopf\_01/generate\_kicad\_pcb.py}

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{hopf_01_knot_traces.png}
    \caption{\textbf{HOPF-01 Wire-Stitched Knot Geometry:} Projected torus knot paths with stitching hole locations for all five knots. The $(2,3)$ trefoil has 3 true crossings; the $(3,5)$ knot has 10; the $(3,11)$ knot has 22.}
    \label{fig:hopf_01_knot_traces}
\end{figure}

\section{Standard Model Baseline Response}

Before any experimental measurement, we require a quantitative classical prediction. What resonant frequencies, quality factors, and return loss does standard Maxwell electrodynamics predict for each wire-stitched knot? The answer establishes the baseline from which any AVE anomaly must be measured.

\subsection{Model Hierarchy}

The SM baseline is constructed in four layers, each representing a distinct classical electromagnetic effect:

\begin{enumerate}
    \item \textbf{Layer 1 --- Dipole self-impedance.} The primary resonance arises from the standing-wave mode of an end-fed thin-wire resonator. Using the King--Middleton EMF method (Balanis, \S8.4), the center-fed input impedance is
    \begin{align}
        R_{in} &= \frac{\eta}{2\pi}\!\left[C_E + \ln(kL) - \mathrm{Ci}(kL) + \tfrac{1}{2}\sin(kL)\bigl(\mathrm{Si}(2kL) - 2\mathrm{Si}(kL)\bigr) \right. \nonumber \\
        &\qquad\quad \left. + \tfrac{1}{2}\cos(kL)\bigl(C_E + \ln(kL/2) + \mathrm{Ci}(2kL) - 2\mathrm{Ci}(kL)\bigr)\right] \\
        X_{in} &= \frac{\eta}{4\pi}\!\left[2\mathrm{Si}(kL) + \cos(kL)\bigl(2\mathrm{Si}(kL) - \mathrm{Si}(2kL)\bigr) \right. \nonumber \\
        &\qquad\quad \left. - \sin(kL)\bigl(2\mathrm{Ci}(kL) - \mathrm{Ci}(2kL) - \mathrm{Ci}(2ka^2/L)\bigr)\right]
    \end{align}
    where $\eta = Z_0/\sqrt{\varepsilon_{eff}}$ is the wave impedance in the dielectric, $k = 2\pi f\sqrt{\varepsilon_{eff}}/c$, $C_E \approx 0.5772$ is Euler's constant, $\mathrm{Si}$ and $\mathrm{Ci}$ are the sine and cosine integrals, and $a$ is the wire radius. For end-fed excitation (SMA at wire start), the standing-wave current node at the end transforms the impedance by a factor $1/\sin^2(kL/2)$.

    \item \textbf{Layer 2 --- Skin-effect ohmic loss.} At $\sim$1~GHz, the copper skin depth is $\delta \approx 2\;\mu\text{m}$. The AC resistance per unit length is $R_{ac} = 1/(\sigma_{Cu} \cdot A_{skin})$, where $A_{skin} = \pi(a^2 - (a-\delta)^2)$. This adds a real impedance $R_{loss} = R_{ac} \cdot L_{wire}$ that broadens the resonance.

    \item \textbf{Layer 3 --- Curvature effective-length correction.} The exact 3D torus knot geometry is generated from $x(t) = (R + r\cos qt)\cos pt$, scaled to the physical wire length. The RMS curvature $\kappa_{rms}$ produces an effective-length correction $L_{eff} = L(1 + a^2\kappa_{rms}^2/4)$. This effect is negligible: $<0.03\%$ for all five knots.

    \item \textbf{Layer 4 --- Crossing mutual coupling.} At each crossing, standard Neumann mutual inductance couples the two wire segments:
    \begin{equation}
        M_{cross} = \frac{\mu_0 \ell}{2\pi}\ln\!\left(\frac{\ell}{d_{sep}}\right) \cdot \cos\theta
        \label{eq:m_cross}
    \end{equation}
    where $\ell \approx 3$~mm is the coupling length (one hole spacing), $d_{sep} \approx 2.1$~mm is the z-separation (PCB thickness + wire diameter), and $\theta$ is the crossing angle. For torus knots, the average crossing angle is $\sim$70$^\circ$ ($\cos\theta \approx 0.34$), which strongly suppresses the mutual inductance. The fractional frequency perturbation is
    \begin{equation}
        \frac{\Delta f}{f}\bigg|_{cross} \approx -\frac{1}{2}\frac{\sum_i M_i}{L_{self}}
    \end{equation}
    where $L_{self} = (\mu_0 L/2\pi)[\ln(2L/a) - 1]$ is the wire self-inductance.
\end{enumerate}

\subsection{SM Baseline Predictions}

Table~\ref{tab:sm_baseline} summarizes the full SM prediction for each antenna in air ($\varepsilon_{eff} = 1.295$). The resonant frequency $f_{SM}$ is the dipole resonance frequency including curvature correction; the crossing coupling perturbation is reported separately as a diagnostic.

\begin{table}[ht]
\centering
\caption{Standard Model baseline predictions (air). $f_{SM}$ is the dipole resonance frequency; $\Delta_{cx}$ is the classical crossing coupling perturbation; $Q$ is the quality factor; S$_{11,min}$ is the minimum return loss.}
\label{tab:sm_baseline}
\begin{tabular}{|l|c|c|c|c|c|c|}
\hline
Antenna & $pq/(p{+}q)$ & $f_{SM}$ (GHz) & S$_{11}$ (dB) & $Q$ & $\Delta_{cx}$ (ppm) & $Z_{in}$ ($\Omega$) \\ \hline
$(2,3)$ Trefoil    & 1.200 & 1.047 & $-24.0$ & 63 & $-767$  & $57 - 1j$ \\
$(2,5)$ Cinquefoil & 1.429 & 0.787 & $-23.4$ & 64 & $-914$  & $57 - 1j$ \\
$(3,5)$            & 1.875 & 0.740 & $-23.3$ & 63 & $-1703$ & $57 - 2j$ \\
$(3,7)$            & 2.100 & 0.630 & $-23.0$ & 64 & $-1975$ & $57 - 2j$ \\
$(3,11)$           & 2.357 & 0.505 & $-22.6$ & 64 & $-2397$ & $58 - 2j$ \\
CONTROL            & 0.000 & 1.047 & $-24.0$ & 63 & $0$     & $57 - 1j$ \\ \hline
\end{tabular}
\end{table}


\subsection{SM vs.\ AVE Prediction Comparison}

The critical question:\ how does the SM baseline differ from the AVE prediction? Table~\ref{tab:sm_vs_ave} compares the two models directly. The AVE chiral correction shifts each resonant frequency by $f_{AVE} = f_{SM}/(1 + \alpha \cdot pq/(p+q))$.

\begin{table}[ht]
\centering
\caption{SM baseline vs.\ AVE prediction. The crossing coupling perturbation ($\Delta_{cx}$) is the SM ``background noise''; the AVE signal is the predicted chiral shift. SNR = AVE signal / classical crossing noise.}
\label{tab:sm_vs_ave}
\begin{tabular}{|l|c|c|c|c|c|}
\hline
Knot & $f_{SM}$ (GHz) & $f_{AVE}$ (GHz) & $\Delta$(SM$\to$AVE) & AVE (ppm) & SNR \\ \hline
$(2,3)$  & 1.047 & 1.038 & $9.1$~MHz  &  8{,}681 & $11\times$ \\
$(2,5)$  & 0.787 & 0.778 & $8.1$~MHz  & 10{,}317 & $11\times$ \\
$(3,5)$  & 0.740 & 0.730 & $10.0$~MHz & 13{,}498 & $8\times$  \\
$(3,7)$  & 0.630 & 0.621 & $9.5$~MHz  & 15{,}093 & $8\times$  \\
$(3,11)$ & 0.505 & 0.496 & $8.5$~MHz  & 16{,}910 & $7\times$  \\
CONTROL  & 1.047 & 1.047 & $0.0$~MHz  &        0 & ---        \\ \hline
\end{tabular}
\end{table}

\noindent The AVE chiral signal (8.1--10.0~MHz shift) exceeds the classical crossing coupling noise by $7$--$11\times$ across all knots. Crucially, the two effects have different functional dependences: the AVE shift scales as $pq/(p+q)$ (sublinear, topological invariant) while the classical coupling scales as $N_{cross}/L_{self}$ (superlinear, geometry-dependent). The zero-topology control antenna provides a direct null test.

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{hopf_01_sm_baseline.png}
    \caption{\textbf{Standard Model Baseline:} SM electromagnetic response for all HOPF-01 antennas computed via the dipole self-impedance + crossing coupling model. Top left: S$_{11}$(f) in air. Top right: S$_{11}$(f) in mineral oil. Middle left: SM vs.\ AVE predicted resonant frequencies. Middle right: classical crossing coupling vs.\ AVE signal magnitude. Bottom left: input impedance Re($Z_{in}$). All curves are pure classical Maxwell --- no AVE corrections applied.}
    \label{fig:hopf_01_sm_baseline}
\end{figure}

\section{Manufacturing Tolerance Rejection}

A critical concern is whether the predicted chiral shift could be mimicked by manufacturing tolerances. A Monte Carlo analysis with $N = 5{,}000$ trials per knot sweeps over the noise sources specific to the wire-stitched form factor:
\begin{itemize}
    \item Wire length tolerance: $\pm 0.5$~mm (hand threading through holes)
    \item Wire height variance: $\pm 0.3$~mm (sag between stitching holes)
    \item SMA connector repeatability: $\pm 200$~kHz feed-point noise
\end{itemize}

The key insight is that these variations affect all six antennas as \textbf{common-mode noise}. The chiral shift is a \textit{differential} measurement that cancels common-mode errors. The residual noise is $\sim$130~kHz per knot, while the chiral signal exceeds 8.5~MHz for all knots, yielding SNR $> 74\sigma$.

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{hopf_01_wire_sensitivity.png}
    \caption{\textbf{Wire-Stitched Sensitivity Analysis:} Comparison of chiral shift distributions in air ($\varepsilon_{eff} = 1.295$) and mineral oil ($\varepsilon_{eff} = 2.265$) for all five knots. The fractional shift $\Delta f / f$ is identical in both media, confirming substrate independence. All knots exceed $74\sigma$ detection threshold.}
    \label{fig:hopf_01_sensitivity}
\end{figure}

\section{Substrate Independence: Air vs.\ Mineral Oil}

The strongest falsification criterion is substrate independence. If the chiral coupling is a vacuum property (Axiom~1), then the \textit{fractional} shift $\Delta f / f$ must be identical regardless of the surrounding medium:
\begin{equation}
    \frac{\Delta f}{f} = \alpha \frac{pq}{p+q} \quad \text{(independent of } \varepsilon_{eff} \text{)}
\end{equation}

The wire-stitched design enables a direct test: the \textit{same board} is measured first in air ($\varepsilon_{eff} = 1.295$), then submerged in mineral oil ($\varepsilon_{r} \approx 2.1$, $\varepsilon_{eff} \approx 2.265$). The absolute frequencies shift downward in oil (expected): the trefoil drops from 1.098~GHz to 0.830~GHz. But the fractional shift $\Delta f / f$ must remain constant. The simulation confirms: across all five knots, the ratio $\Delta f/f|_{air} \;/\; \Delta f/f|_{oil} = 1.00000$ to machine precision.

This eliminates the need for multiple expensive PCB substrates (Rogers, duroid). A single \$10 FR-4 board tested in two media provides an equally powerful substrate independence check.

\section{Bill of Materials}

\begin{center}
\begin{tabular}{|l|c|c|}
\hline
Item & Qty & Est.\ Cost \\ \hline
PCB 160$\times$120~mm, 2L FR-4, ENIG (JLCPCB, 5 pcs)           & 1 lot & \$12  \\
SMA edge-launch connectors (TE/Linx CONSMA003.062)              & 6     & \$34  \\
50$\Omega$ SMA terminators                                      & 5     & \$15  \\
Enameled magnet wire, 24~AWG, 1~lb spool (Remington)            & 1     & \$14  \\
M3 hardware kit + 10~mm nylon standoffs                         & 1 kit & \$8   \\
SMA-to-SMA cable, 30~cm, RG316                                  & 1     & \$8   \\
Mineral oil, 500~mL, transformer grade                          & 1     & \$10  \\
Glass dish, 9$\times$13'' (Pyrex, oil bath)                     & 1     & \$10  \\ \hline
\textbf{Subtotal (excl.\ VNA)}                                   &       & \textbf{\$111} \\ \hline
LiteVNA-64 (50~kHz -- 6.3~GHz, incl.\ cal kit)                  & 1     & \$100 \\ \hline
\textbf{Grand Total}                                             &       & \textbf{\$211} \\ \hline
\end{tabular}
\end{center}

\section{Classical Coupling as Confounding Variable}

Before claiming an AVE anomaly, we must address an obvious classical confound: at each wire crossing, standard Maxwell electrodynamics predicts parasitic mutual inductance and capacitance between the two wire segments. These crossings shift the resonant frequency by an amount that depends on the number of crossings, which correlates with knot complexity.

The crossing numbers for the five HOPF-01 knots are 3, 5, 10, 14, and 22 respectively (from the minimum crossing number formula $c(p,q) = \min\bigl(p(q{-}1),\; q(p{-}1)\bigr)$). The key question is: does this classical coupling have the \textit{same functional form} as the AVE prediction?

\begin{center}
\begin{tabular}{|c|c|c|c|c|}
\hline
Knot & $pq/(p{+}q)$ & $N_{cross}$ & AVE (norm) & Classical (norm) \\ \hline
$(2,3)$  & 1.200 & 3  & 1.00 & 1.00 \\
$(2,5)$  & 1.429 & 5  & 1.19 & 1.31 \\
$(3,5)$  & 1.875 & 10 & 1.56 & 2.49 \\
$(3,7)$  & 2.100 & 14 & 1.74 & 3.04 \\
$(3,11)$ & 2.357 & 22 & 1.95 & 3.96 \\ \hline
\end{tabular}
\end{center}

\noindent The scaling shapes are \textbf{fundamentally different}: AVE scales smoothly as $pq/(p{+}q)$ (sublinear, ratio 1.95$\times$ across the range), while classical coupling scales as $N_{cross}/L_{self}$ (superlinear, ratio 3.96$\times$). An experiment with five knots plus a zero-topology control can distinguish these functional forms by model selection (AIC/BIC).

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{hopf_01_classical_coupling.png}
    \caption{\textbf{Classical vs.\ AVE coupling:} Left: absolute shift magnitudes (note classical estimates are crude and likely overestimated by the perturbation model). Right: normalized scaling shapes---the classical coupling (red squares) scales superlinearly with crossing density, while the AVE prediction (green circles) scales sublinearly as $pq/(p{+}q)$. This difference is experimentally distinguishable.}
    \label{fig:hopf_01_classical_coupling}
\end{figure}

Additionally, a \textbf{zero-topology control antenna} provides a direct test: a wire with the same total length as the trefoil (120~mm) but routed as a meander or figure-8 (with artificial crossings but zero topological winding number $pq/(p{+}q) = 0$). If this control shows:
\begin{itemize}
    \item Zero chiral shift $\rightarrow$ the effect is topological (supports AVE)
    \item Nonzero shift $\rightarrow$ the effect is from crossings (classical artifact)
\end{itemize}
\subsection{Additional Discriminating Observables}

Beyond the primary resonant frequency shift, several independent observables provide further separation between the SM and AVE predictions. Each test is falsifiable and requires no additional hardware beyond the HOPF-01 board and VNA.

\begin{enumerate}
    \item \textbf{Harmonic structure.} The SM predicts harmonics at the usual series $f_n = n \cdot c/(2L\sqrt{\varepsilon_{eff}})$. If the AVE chiral coupling is a bulk refractive index modification (Eq.~\ref{eq:n_ave}), then \textit{all} harmonics must shift by the same fractional $\Delta f/f = \alpha \cdot pq/(p+q)$. Classical crossing coupling, by contrast, produces frequency-dependent perturbations (mutual impedance scales as $\omega M$), so the fractional shift would differ at each harmonic. Measuring the first three harmonics per knot provides an independent consistency check.

    \item \textbf{Quality factor vs.\ topology.} The SM predicts that $Q$ depends only on ohmic loss and radiation resistance---both geometry-dependent but topology-independent for wires of the same length. The AVE prediction does not add a dissipative term (chiral coupling modifies the \textit{real} part of $n$, not the imaginary part), so $Q$ should be unchanged. If the measured $Q$ systematically varies with $pq/(p+q)$, it would indicate an unmodeled parasitic effect, not chiral coupling.

    \item \textbf{Temperature stability.} Both SM and AVE predictions are temperature-independent: $\mu_0$, $\alpha$, and $pq/(p+q)$ do not change with temperature. However, if the observed shift were due to \textit{thermoelectric} artifacts (Seebeck voltages at SMA--wire junctions, thermal expansion of wire length), then it would vary with ambient temperature. Repeating the measurement at 5$^\circ$C and 35$^\circ$C brackets the thermal noise floor.

    \item \textbf{Enantiomeric handedness.} A $(p,q)$ torus knot can be threaded in two mirror-image configurations (left-handed and right-handed). Standard Maxwell coupling is parity-invariant: both enantiomers produce identical $M$ and $C$ at each crossing. The AVE chiral coupling, however, derives from the \textit{signed} winding number and could in principle produce a handedness-dependent shift. Threading two trefoils of opposite handedness on the same board tests this directly.

    \item \textbf{Wire length scaling.} Building the same $(2,3)$ trefoil topology at two different physical lengths (e.g., 120~mm and 240~mm, by doubling the hole spacing) provides a powerful test. The crossing number is fixed by topology ($N_{cross} = 3$ for both), but $L_{self}$ doubles. The SM crossing perturbation scales as $M/L_{self}$, so it \textit{halves}. The AVE prediction $\Delta f/f = \alpha \cdot pq/(p+q)$ is length-independent. This separates geometric from topological effects.

    \item \textbf{Radiation pattern polarization.} A torus knot antenna is intrinsically chiral: its radiation pattern has a circularly polarized component whose handedness matches the knot. The SM predicts this polarization from the wire geometry alone. The AVE framework predicts no additional polarization modification (the chiral coupling is scalar, not vectorial). Measuring the axial ratio with a reference horn provides an orthogonal test of the underlying physics.
\end{enumerate}

Table~\ref{tab:discriminators} summarizes the predicted behavior of each observable under both models.

\begin{table}[ht]
\centering
\caption{Predicted behavior of discriminating observables under SM and AVE models. ``Same'' indicates the observable does not distinguish the models; ``Different'' indicates a falsifiable difference.}
\label{tab:discriminators}
\begin{tabular}{|l|c|c|c|}
\hline
Observable & SM prediction & AVE prediction & Discriminating? \\ \hline
$\Delta f/f$ at harmonics     & frequency-dependent         & constant (same ppm)         & \textbf{Yes} \\
$Q$ vs.\ topology              & topology-independent        & topology-independent        & No (null test)  \\
Temperature dependence         & none                        & none                        & No (null test)  \\
Enantiomeric shift             & identical                   & potentially different       & \textbf{Yes}$^*$ \\
Wire length scaling            & $\propto 1/L$               & constant                    & \textbf{Yes} \\
Radiation polarization         & geometry-determined         & geometry-determined          & No (null test)  \\ \hline
\multicolumn{4}{l}{\footnotesize $^*$Depends on whether chiral coupling has a signed or unsigned winding number.}
\end{tabular}
\end{table}

\noindent The null tests (items 2, 3, 6) are valuable because they constrain the \textit{absence} of systematic errors. If $Q$ varies with topology or the shift changes with temperature, the measurement is contaminated regardless of whether AVE is correct. The positive discriminators (items 1, 4, 5) provide independent falsification tests that can be performed with the existing HOPF-01 hardware.

\section{Decision Gate}

If the HOPF-01 scaling law is confirmed---meaning $\Delta f/f$ is linear in $pq/(p+q)$ with slope $\alpha$, identical in air and oil, and absent in the zero-topology control---the AVE framework has produced a genuine, zero-parameter electromagnetic prediction that no existing Maxwell solver can reproduce. This justifies the significantly greater investment required for the mechanical thrust measurements described in the following chapters.

If the scaling law is \textit{not} confirmed, the chiral coupling term in Eq.~\ref{eq:n_ave} is falsified, and the PONDER thrust predictions must be re-examined from first principles.
